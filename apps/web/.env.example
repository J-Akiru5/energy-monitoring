# Shared secret for relay actuation. Must be set to the same value in both
# apps' Vercel project settings. POST /api/relay rejects requests without a
# matching X-Relay-Secret header.
RELAY_ADMIN_SECRET=
