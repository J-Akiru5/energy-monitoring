# Environment Variables for Energy Monitoring System

# ──── Supabase ───────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ──── Device Auth ────────────────────────────────
# Secret token for ESP32 → API authentication
DEVICE_API_KEY=your-device-secret-key

# ──── Billing ────────────────────────────────────
DEFAULT_RATE_PHP_PER_KWH=10.00

# ──── Mock Sensor (dev only) ─────────────────────
MOCK_API_URL=http://localhost:3000
MOCK_DEVICE_TOKEN=dev-test-token
MOCK_INTERVAL_MS=2000
